version: "3.9"

services:

  backend:
    build:
      context: ./backend
    container_name: intellmeet-backend

    ports:
      - "5000:5000"

    env_file:
      - ./backend/.env

    restart: unless-stopped

  frontend:
    build:
      context: ./frontend

    container_name: intellmeet-frontend

    ports:
      - "3000:80"

    depends_on:
      - backend

    restart: unless-stopped